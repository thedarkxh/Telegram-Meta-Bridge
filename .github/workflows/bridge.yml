name: Social News Bridge

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'last_msg_id.txt'
  schedule:
    - cron: '*/30 * * * *' # Run every 30 minutes
  workflow_dispatch: # Allow manual trigger

jobs:
  bridge:
    runs-on: ubuntu-latest
    
    # Explicitly grant write permissions so the workflow can commit the state file
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          persist-credentials: true # Crucial for push step to use the workflow's write permission

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install telethon requests

      - name: Run bridge
        env:
          TG_API_ID: ${{ secrets.TG_API_ID }}
          TG_API_HASH: ${{ secrets.TG_API_HASH }}
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_SOURCE_CHANNEL: ${{ secrets.TG_SOURCE_CHANNEL }}
          FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
          IG_USER_ID: ${{ secrets.IG_USER_ID }}
          FB_ACCESS_TOKEN: ${{ secrets.FB_ACCESS_TOKEN }}
        run: python bridge.py

      - name: Commit and push state
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          git add last_msg_id.txt
          git commit -m "Update last processed message ID [skip ci]" || echo "No changes to commit"
          git push
